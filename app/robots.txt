export default function robots() {
  return {
    rules: [
      {
        userAgent: '*',
        allow: '/',
        disallow: [
          '/api/',
          '/dashboard/', // Protects private user health data
          '/admin/',
          '/*?plan=',    // Prevents indexing of checkout/conversion URLs
        ],
      },
    ],
    sitemap: 'https://pelvi.health/sitemap.xml',
  }
}
